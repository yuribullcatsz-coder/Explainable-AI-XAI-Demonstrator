[pytest]
testpaths = tests
pythonpath = .
markers =
    slow: optional tests that load SHAP/LIME/matplotlib heavily
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
